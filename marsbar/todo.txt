To do list
----------

Fields in spmD used by marsbar estimatation
mars_stat
spm99/mars      spm2
xX		
xX.X
xX.xVi          xVi
xX.xVi.Vi	xVi.Vi
xX.K
xX.Xnames     - xX.name 

Fields added
xX.V            xVi.V
xX.xKXs                      %-Filtered design matrix
xX.pKX     
xX.pKXV	       %-for contrast variance weight
xX.Bcov	       %-Variance of est. param.
xX.trRV        % 
xX.trRVRV      %-Variance expectations
xX.nKX         % scaled design matrix for display 
[xCon]

check images function.

add pairs of get/set functions for mardo 

Remove mars_mm_model, I guess
Move pre_release, make_contents into 'release' directory
Script to fish out version, do CVS export, and pack
Test rig with various models for estimation 
grep out is_there, remove is_there function
(rgrep -R "*.m' is_there .)
move mars_get_region into marsbar, remove (only used in marsbar.m)
add 'mm' as synonym for 'real' in marois, and marsy's
add add_contrast function to mardo methods
(should check if contrast is already present, return indices into xCon
when adding e.g

[D Ic] = add_contrasts(D, names, stat_types, values)
OR
[D Ic] = add_contrasts(D, names, stat_types, values, con_types)

SPMest.xCon(end + 1)   = spm_FcUtil('Set','stim_hrf',...
				   'T','c',[1 0 0]',SPMest.xX.xKXs);

contrast = spm_FcUtil('Set',names{n}, types{n}, 'c', values{n}', sX);
if isempty(xCon),xCon = contrast;
else
iFc2 = spm_FcUtil('In', contrast, sX, xCon);
     if ~iFc2, 
       xCon(length(xCon)+1) = contrast;
     else 
       %- 
       fprintf('\ncontrast %s (type %s) already in xCon', names{n}, types{n});
     end
   end

remove all traces of spm batch mode (sorry JB). 
edit make_contents.m

marsy
add functions as listed
move get_marsy to marsy in maroi/, add VY, design 
add voxel coordinate return to getdata
change references to marsY in marsbar functions to use methods
